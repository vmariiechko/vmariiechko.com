<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title or site.title }}</title>
    <meta name="description" content="{{ description or site.description }}" />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body class="bg-gray-100 text-gray-800 antialiased">
    <div class="container mx-auto max-w-3xl p-4">
      <header class="mb-8">
        <h1 class="text-3xl font-bold">
          <a href="/" class="text-gray-900 hover:text-blue-600">{{ site.title }}</a>
        </h1>
        <p class="text-gray-600">{{ site.description }}</p>
      </header>

      <main>
        {{ content | safe }}
      </main>

      <footer class="mt-12 pt-6 border-t border-gray-300 text-center text-sm text-gray-500">
        <p>&copy; {% year %} {{ site.author.name }}. All rights reserved.</p>
      </footer>
    </div>

    <!-- Umami analytics script will be injected here -->
  </body>
</html>
